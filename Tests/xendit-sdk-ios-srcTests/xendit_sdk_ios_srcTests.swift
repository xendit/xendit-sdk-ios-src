import XCTest
@testable import xendit_sdk_ios_src

final class xendit_sdk_ios_srcTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(xendit_sdk_ios_src().text, "Hello, World!")
    }
}
