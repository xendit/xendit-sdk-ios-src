
source 'https://github.com/CocoaPods/Specs.git'
target 'Xendit' do
    use_frameworks!

    inherit! :search_paths
    platform :ios, '8.0'
    
    pod 'FMDB', '2.6.2'
    pod 'ReachabilitySwift'

    pod 'SnowplowTracker'

    target 'XenditTests' do
        inherit! :search_paths
        # Pods for testing
    end

    target 'XenditUniversal' do
        inherit! :search_paths
        # Pods for testing
    end
end
